
// Reading the Data from an Array 

    // let myArray: (number | string | boolean)[] = [100, "Adam", true];

    // let val = myArray[0];
    // console.log(`Value: ${val}`)

// replace value

let myArray: (number | string | boolean)[] = [100, "Adam", true];

myArray[0] = "Tuesday";
let val = myArray[0];
console.log(`Value: ${val}`);
