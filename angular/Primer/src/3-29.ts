let place: string | undefined | null;
place = "Dhaka";
console.log(`Place value: ${place} Type: ${typeof(place)}`); // backteack ` use
console.log("Place value:" + place + " Type: " + typeof(place)); // without backteack `, genaral concate +

